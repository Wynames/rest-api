// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,           // deteksi masalah lebih awal
  // (opsional) di masa depan bisa ditambahkan images, i18n, dll.
};

export default nextConfig;
